[project]
name = "pyuvcgen"
version = "0.1.0"
description = "Python-based UVM UVC generator using Jinja2 templates"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Ciro Bermudez", email = "cirofabian.bermudez@gmail.com"}
]
requires-python = ">= 3.9"
keywords = ["uvm", "verification", "code-generator", "jinja2"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Code Generators",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "Jinja2 >= 3.1.6",
    "PyYAML >= 6.0.2"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
pyuvcgen = "pyuvcgen.__main__:main"

[project.urls]
Homepage = "https://github.com/uvmcollab/"
Repository = "https://github.com/uvmcollab/pyuvcgen"
Issues = "https://github.com/uvmcollab/pyuvcgen/issues"
